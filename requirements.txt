# Python dependencies for the Card & Payment Fraud Detection POC.
# Target: Python 3.12. Install with:  pip install -r requirements.txt
#
# The pipeline uses scikit-learn only (no xgboost/lightgbm). numpy is pulled in
# by pandas/scikit-learn but is also imported directly, so it is pinned here.

pandas>=2.1
scikit-learn>=1.3.2
joblib>=1.3
numpy>=1.26
